export const fetchPlaylists = () => {
  return({
    method: "GET",
    url: `api/playlists`
  });
};

export const fetchPlaylist = (id) => {
  return({
    method: "GET",
    url: `api/playlists/${id}`
  });
};

export const fetchPlaylist = (id) => {
  return({
    method: "GET",
    url: `api/playlists/${id}`
  });
};